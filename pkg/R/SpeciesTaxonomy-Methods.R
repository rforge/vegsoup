#data(testdata)
#dta <- VegsoupData(Vegsoup(species, sites, taxonomy, "braun.blanquet"))
#foo <- Species(dta)
#names(foo)[1] <- "bar"
#bar <- new("Species", data = foo)

#foo < -new("SpeciesTaxonomy",
#	species = new("Species", data = Species(dta)),
#	taxonomy = new("Taxonomy", data = Taxonomy(dta)))