improve Spread()
man files for coordiantes, bbox, proj4string
noquote for print method (e.g. as.matrix)
Richness by Partiton
#	aggregate(as.logical(prt), by = list(Partitiong(prt), sum)

extend slot parttiton to contain class dendrogram

# minor
rename
layer to stratum
cov to abu(dance)

# ideas
rle() can be usefull

