findcoordiantes()

rename Species to species?
and Sites to sites?

improve Spread()
man files for coordiantes, bbox, proj4string
noquote for print method (e.g. as.matrix)
extend slot partition to contain class dendrogram

# minor

csv2 = TRUE to sep and dec

tools::showNonASCII(readLines(f))
rename
layer to stratum
cov to abu(dance)

# ideas
rle() can be usefull

